server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${instanceId:${random.value}}

echo:
  message:
    text: 'Hello, World! Aryan !!'

spring:
  application:
    name: spring-cloud-bus-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
    bus:
      enabled: true
      destination: TP.POC.CONFIG.REFRESH
      id: ${spring.application.name}:${instanceId:${random.value}}
      trace:
        enabled: true
      refresh:
        enabled: true
      env:
        enabled: true
  kafka:
    bootstrap-servers: localhost:29092

management:
  endpoints:
    web:
      exposure:
        include: busrefresh,health,info,env,metrics
